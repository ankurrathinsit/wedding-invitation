version: 1
frontend:
  phases:
    build:
      commands: []
  artifacts:
    # Static site — publish the repo root as-is (no build step).
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
